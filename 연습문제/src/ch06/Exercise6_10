Exercise6_10

Q) 생성자의 설명으로 틀린 것은?

a. 모든 생성자의 이름은 클래스의 이름과 동일 O -> 오버로딩이라서
b. 생성자는 객체를 생성하기 위한 것 X -> 객체 생성은 new, 생성자는 인스턴스 멤버 초기화를 위한 것
c. 클래스에는 생성자가 반드시 하나 이상 있어야 함 O -> 없으면 컴파일러가 기본 생성자 추가해줌
d. 생성자가 없는 클래스는 컴파일러가 기본 생성자를 추가 O
e. 생성자는 오버로딩 불가능 X -> 매개 변수의 타입이나 개수가 다르게 오버로딩 가능

A) b, e