Exercise6_14

Q) 초기화에 대한 설명으로 틀린 것은?
 
a. 멤버 변수는 자동 초기화되므로 초기화하지 않고도 값을 참조 가능 O -> 자동으로 타입에 맞게 초기화 됨
b. 지역 변수는 사용하기 전에 반드시 초기화 해야 함 O
c. 초기화 블럭보다 생성자가 먼저 수행됨  X -> 생성자는 인스턴스 초기화의 마지막
d. 명시적 초기화를 제일 우선적으로 고려해야 함 O -> 가장 간단한 초기화 방법
e. 클래스 변수보다 인스턴스 변수가 먼저 초기화 됨 X -> 클래스 변수는 실행 시 바로 로드 됨

A) c, e