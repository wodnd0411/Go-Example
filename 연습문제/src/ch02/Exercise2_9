Exercise2_9

Q) 형변환 생략 가능한 것은?

byte b = 10;
char ch = 'A';
int i = 100;
long l = 1000L;

a. b = (byte)i; // 작은 범위로 형변환 -> 생략 불가능
b. ch = (char)b; // 다른 범위 -> 생략 불가능 
c. short s = (short)ch; // 같은 크기, 다른 범위 -> 생략 불가능
d. float f = (float)l; // 큰 범위로 형변환 -> 가능
e. i = (int)ch; // 큰 범위로 형변환 -> 가능

A) d, e